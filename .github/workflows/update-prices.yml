name: Actualizare preturi ceasuri

on:
  workflow_dispatch:

# Workflow dezactivat din schedule — preturile se actualizeaza local
# cu node scrape-promotii.cjs, apoi products.json se urca manual pe GitHub.
# deploy.yml face build + deploy automat la fiecare push pe main.

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Preturile se actualizeaza local. Urca products.json pe GitHub si deploy.yml face build automat."
